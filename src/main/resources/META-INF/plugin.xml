<!-- url="" specifies the URL of the plugin homepage (displayed in the Welcome Screen and in "Plugins" settings dialog) -->
<idea-plugin url="http://www.jetbrains.com/idea">

    <name>Definitions</name>

    <id>com.main.Definitions</id>

    <description>Fun with definitions in intellij. This must be at least 40 characters.</description>

    <change-notes>There are lots of changes. This must be at least 40 characters.</change-notes>

    <version>1.0.19</version>

    <vendor url="http://www.jetbrains.com" email="support@jetbrains.com" />

    <idea-version since-build="" until-build=""/>

    <actions>
        <action id="generateDialog" class="com.components.actions.PopUpAction" text="Get Definition" description="yo yo yo">
            <add-to-group group-id="EditorPopupMenu" anchor="first"/>
        </action>
        <action id="DefinitionsToolMenu" class="com.components.actions.ToolsMenuAction" text="Definitions Plugin"
                description="This action allows the user to toggle the definitions they want. Defaults to Java on and all others off.">
            <add-to-group group-id="ToolsMenu" anchor="first"/>
        </action>
    </actions>

    <extensions defaultExtensionNs="com.intellij"></extensions>

</idea-plugin>
